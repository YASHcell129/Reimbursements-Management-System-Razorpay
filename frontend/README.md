# Frontend

Open the file [index.html](index.html) in a browser, or serve the folder using a simple static server.

For a quick local run:

```bash
cd frontend
python -m http.server 8080
```

Then open http://localhost:8080.
